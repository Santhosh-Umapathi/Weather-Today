import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {paddingHorizontal: 24, paddingTop: 20, gap: 8},
  listStyle: {position: 'absolute', top: 20, width: '100%'},
});
