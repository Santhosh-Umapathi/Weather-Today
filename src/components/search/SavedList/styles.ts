import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {paddingHorizontal: 24, paddingTop: 20, gap: 8, flexGrow: 1},
  listStyle: {
    position: 'absolute',
    top: 20,
    width: '100%',
    backgroundColor: 'red',
    zIndex: 10,
  },
});
